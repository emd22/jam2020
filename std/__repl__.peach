# Handy Repl functions and variables

func tictactoe() {
  import "examples/ttt.peach";

  ttt.new().play();
}

let sep_str = '\n' + (' ' * 22) + ('-' * 30) + '\n';

io.write(sep_str);
io.write_color(Console.RED, ' *** ');
io.write_color(Console.BOLD, "Run `tictactoe();` to start a game of TicTacToe (built 100% in PEACH!)");
io.write_color(Console.RED, ' *** ');
io.write('\n');
io.write((' ' * 8) + 'Code in examples/ttt.peach. More examples in that folder as well!');
io.write(sep_str);
io.write('\n');